-- Person entities removed; identity is User-only (registration on landing).
-- Invitation-by-username removed; owners approve registered users without membership.

DROP TABLE IF EXISTS persons;

DROP TABLE IF EXISTS installation_invitations;
