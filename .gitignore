.DS_Store
.env
.env.*
!.env.example
dist/
build/
node_modules/
*.log
backups/
.direnv/
.envrc
.vscode/
.cursor/
.idea/

# Rust
/target/

# Vitest / Vite
apps/web/.vite
apps/web/dist/
apps/web/node_modules/
apps/web/package-lock.json

# Claude local settings (personal, not project docs)
.claude/settings.local.json

# Worktrees efímeros de agentes (cada uno es un clon completo del repo, con su propio
# target/). Sin esta línea, un `git add -A` los mete como repos embebidos — pasó el
# 2026-08-28 durante la Fase 1 del issue #82 y hubo que sacarlos del índice a mano.
# Además guardaban 7,3 GB en sus target/, invisibles para `docker system df`.
.claude/worktrees/
