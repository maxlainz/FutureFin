name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: cargo build
        run: cargo build -p futurefin-api --locked
      - name: cargo test (engine — no DB required)
        run: cargo test -p futurefin-engine --locked

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: npm install + typecheck + build
        run: |
          npm install
          npm run typecheck:web
          npm run build:web

  # Imagen API + Compose: detecta Dockerfile roto, entrypoint faltante y arranque contra Postgres.
  docker-stack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Build API image
        run: docker build -t futurefin-ci:latest -f apps/api/Dockerfile .
      - name: Start Postgres + API
        run: |
          FUTUREFIN_IMAGE=futurefin-ci FUTUREFIN_TAG=latest \
          POSTGRES_PASSWORD=ci-test \
          docker compose up -d
      - name: Wait for /v1/health
        run: |
          set -e
          for i in $(seq 1 90); do
            if curl -sf http://127.0.0.1:8080/v1/health; then
              echo "health ok"
              exit 0
            fi
            sleep 2
          done
          POSTGRES_PASSWORD=ci-test docker compose ps -a
          POSTGRES_PASSWORD=ci-test docker compose logs --tail=100 futurefin || true
          exit 1
      - name: Stop stack
        if: always()
        run: POSTGRES_PASSWORD=ci-test docker compose down -v --remove-orphans
